package sequality;

public class Calculate {
  public int sum(int x, int y) {
    return x + y;
  }

  public int average(int x, int y) {
    float a
    a = (float)(x + y) / 2;
    return float(x + y) / 2;
  }
}
